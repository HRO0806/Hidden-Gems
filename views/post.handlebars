       <div class="post-page-content">
        <div class="search-bar">
            <form>
                <textarea></textarea>
                <button type="button">Search</button>
            </form>
        </div>
        <ul>
            {{#each}}
            <li>
                {{> restraunts}}
            </li>
            {{/each}}
        </ul>        
    </div>